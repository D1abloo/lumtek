# Frontend (Vite)
# En desarrollo con proxy de Vite puedes omitirlo; en producción usa /api/contact
VITE_CONTACT_ENDPOINT=/api/contact

# API / SMTP GoDaddy (Workspace / Professional Email)
PORT=3001
SMTP_HOST=smtpout.secureserver.net
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=juanf.delgado@lumtek.es
SMTP_PASS=tu-contraseña-del-buzón
MAIL_TO=juanf.delgado@lumtek.es

# Si tu correo es Microsoft 365 desde GoDaddy, usa en su lugar:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Origen permitido en CORS (opcional, dev)
# CORS_ORIGIN=http://localhost:5173
