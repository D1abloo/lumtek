# Copia a deploy.env y exporta (no se sube a git):
# set -a && source deploy/deploy.env && set +a && bash deploy/setup-and-deploy.sh

LUMTEK_VPS_IP=203.0.113.10
LUMTEK_VPS_USER=root
LUMTEK_SITE_PREFIX=lumtek
LUMTEK_DEPLOY_PATH=/var/www/lumtek
LUMTEK_CERTBOT_EMAIL=admin@ejemplo.com

# Opcional: dominio distinto de lumtek.IP.nip.io
# LUMTEK_DOMAIN=lumtek.midominio.es
