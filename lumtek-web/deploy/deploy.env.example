# Copia a deploy.env y exporta antes del deploy (no se sube a git):
# set -a && source deploy/deploy.env && set +a && bash deploy/deploy-all.sh

LUMTEK_VPS_IP=31.70.109.174
LUMTEK_VPS_USER=root
LUMTEK_SITE_PREFIX=lumtek
LUMTEK_DEPLOY_PATH=/var/www/lumtek
LUMTEK_DOMAIN=lumtek.es
LUMTEK_CERTBOT_EMAIL=juanf.delgado@lumtek.es

# Build frontend (canonical, Open Graph, JSON-LD)
VITE_SITE_URL=https://lumtek.es

# Opcional: sincronizar SMTP al desplegar API
# LUMTEK_SMTP_PASS=tu_contraseña
