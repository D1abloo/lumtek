# Plantilla Nginx — Lumtek (SPA Vite + SSL con Certbot)
# Ruta en el servidor: /etc/nginx/sites-available/lumtek
#
# Instalación:
#   ln -sf /etc/nginx/sites-available/lumtek /etc/nginx/sites-enabled/lumtek
#   nginx -t && systemctl reload nginx
#   certbot --nginx -d lumtek.31.70.109.174.nip.io

server {
    listen 80;
    listen [::]:80;
    server_name lumtek.31.70.109.174.nip.io;

    # Tras certbot, suele añadirse redirección a HTTPS.
    # Si aún no hay SSL, sirve el sitio en HTTP:
    root /var/www/lumtek;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(?:js|css|png|jpg|jpeg|gif|ico|svg|webp|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public";
        try_files $uri =404;
    }
}

# Certbot añade un bloque server { listen 443 ssl; ... } automáticamente.
# Ejemplo tras emitir el certificado:
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name lumtek.31.70.109.174.nip.io;
#
#     ssl_certificate /etc/letsencrypt/live/lumtek.31.70.109.174.nip.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/lumtek.31.70.109.174.nip.io/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     root /var/www/lumtek;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location ~* \.(?:js|css|png|jpg|jpeg|gif|ico|svg|webp|woff2?)$ {
#         expires 7d;
#         add_header Cache-Control "public";
#         try_files $uri =404;
#     }
# }
